"""
Basic experiment scaffolding for SIT.

Responsibilities:
- Initialize device, seeds, and reproducibility settings.
- Construct the SIT model and related components (optimizer, scheduler).
- Provide simple hooks to run a minimal training/evaluation cycle.

Fill in functions to return configured objects used by `exp_main`.
"""

def setup_experiment(*args, **kwargs):
    """Placeholder experiment setup."""
    raise NotImplementedError("Instantiate model, optimizer, and other components.")
